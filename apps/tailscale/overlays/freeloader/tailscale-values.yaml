apiServerProxyConfig:
  mode: "true" # "true", "false", "noauth"
operatorConfig:
  hostname: freeloader
  defaultTags:
    - tag:k8s-operator-freeloader
  image:
    repository: ghcr.io/tailscale/k8s-operator
proxyConfig:
  defaultTags: tag:k8s-freeloader
  image:
    repository: ghcr.io/tailscale/tailscale
